<base:CustomControlModel  xmlns:base="clr-namespace:THE_LITER_KIOSK.UIManager"
             x:Class="THE_LITER_KIOSK.Controls.TableControl.TableControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:THE_LITER_KIOSK.Controls.TableControl"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="8*" />
            <RowDefinition Height="2*" />
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0">
            <ListView ItemsSource="{Binding TableItems}">

                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="3" Rows="3"/>
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>

                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Background" Value="#e7dff3"/>
                        <Setter Property="Height" Value="240"/>
                        <Setter Property="Width" Value="640"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    </Style>
                </ListView.ItemContainerStyle>
                
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel >
                            <TextBlock Text="{Binding TableIdx, StringFormat={}{0}번}"
                                       FontSize="30"
                                       FontWeight="Bold"
                                       />
                            <TextBlock Text="{Binding TotalPrice, StringFormat={}{0}원}"
                               FontSize="30" />

                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
        
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Button Content="이전" 
                        Width="260"
                        Height="70"
                        HorizontalAlignment="Left"
                        Margin="20 0 0 0"
                        Background="#008575"
                        FontWeight="Bold"
                        FontSize="25"
                        Foreground="White"
                        x:Name="btnTablePrev"
                        Click="btnTablePrev_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="10"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>

            <Grid Grid.Column="1">
                <Button Content="다음"
                        Width="120"
                        Height="70"
                        HorizontalAlignment="Right"
                        Margin="0 0 20 0"
                        Background="#7f7f7f"
                        FontWeight="Bold"
                        FontSize="25"
                        Foreground="White"
                        x:Name="btnMoveToPay"
                        Click="btnMoveToPay_Click"/>
            </Grid>
        </Grid>
     
    </Grid>
</base:CustomControlModel>
